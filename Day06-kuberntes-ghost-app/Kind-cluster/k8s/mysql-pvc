apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: ghost-app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard   # kind's default StorageClass (local-path-provisioner)
  resources:
    requests:
      storage: 2Gi
