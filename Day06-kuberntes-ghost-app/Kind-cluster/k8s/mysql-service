apiVersion: v1
kind: Service
metadata:
  name: ghost-mysql
  namespace: ghost-app
spec:
  selector:
    app: ghost-mysql
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
  clusterIP: None   # headless: gives MySQL a stable DNS name, fine for single-replica StatefulSet-style use
